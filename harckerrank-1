#include <iostream>

using namespace std;

int main() {
    // 1. Declare the variables
    int a, b, c;
    
    // 2. Read the three space-separated integers
    cin >> a >> b >> c;
    
    // 3. Calculate and print the sum
    cout << a + b + c << endl;
    
    return 0;
}
